import React from "react"

export default function SettingsProviders() {
  return (
    <div style={{ padding: 16 }}>
      <h2>Provider 设置</h2>
      <p>读取 .minds/config/providers.json（只读演示）</p>
      <p>后续支持连通性测试（不落盘）。</p>
    </div>
  )
}